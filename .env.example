OPENAI_API_KEY=
APIFY_API_TOKEN=
ANYLIST_EMAIL=
ANYLIST_PASSWORD=

# Anthropic API key used for recipe extraction (Milestone 1)
ANTHROPIC_API_KEY=

# Bearer token for POST /api/import (Milestone 3).
# Generate one with: openssl rand -hex 32
# Must NOT be an Anthropic or AnyList credential.
RECIPE_API_KEY=
